export const GET_AUTH = state => {
    return state.isAuthenticated;
}

export const GET_DUMMY_DATA = state => {
    return state.dummyData;
}

export const GET_CART_DATA = state => {
    return state.cartData;
}
